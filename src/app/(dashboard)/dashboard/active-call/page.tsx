import { ActiveCallInterface } from "@/components/dashboard/active-call-interface";

export const metadata = {
  title: "Active Call | SMB Voice",
  description: "Real-time call management interface",
};

export default function ActiveCallPage() {
  return <ActiveCallInterface />;
}
