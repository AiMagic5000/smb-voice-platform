import { CallTags } from "@/components/dashboard/call-tags";

export const metadata = {
  title: "Call Tags | SMB Voice",
  description: "Call tagging and categorization system",
};

export default function CallTagsPage() {
  return <CallTags />;
}
